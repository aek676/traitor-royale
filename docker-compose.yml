services:
  app:
    build: .
    image: ${DOCKER_IMAGE:-traitor-royale:latest}
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=4321
    ports:
      - "4321:4321"
    restart: unless-stopped